import { Button, Card, CardContent, CardHeader, CardTitle, Field, FieldLabel, FormFeedback, Input, Table, TableBody, TableCell, TableHead, TableHeader, TableRow, useFormFeedback } from '@doscientos/ui'
import { useState } from 'react'
import { addInventoryMovement, createIngredient, replaceRecipe } from '../application/product'

export function ProductPage({ ingredients, stock, menuItems, tenantId, venueId, onDone }: { ingredients: Awaited<ReturnType<typeof import('../application/product').listIngredients>>; stock: Awaited<ReturnType<typeof import('../application/product').getInventory>>; menuItems: { id: string; name: string }[]; tenantId: string; venueId: string; onDone: () => void }) {
  const feedback = useFormFeedback(); const [name, setName] = useState(''); const [unit, setUnit] = useState('kg'); const [cost, setCost] = useState(''); const [minimum, setMinimum] = useState(''); const [ingredientId, setIngredientId] = useState(''); const [movementQuantity, setMovementQuantity] = useState(''); const [movementKind, setMovementKind] = useState<'purchase' | 'waste' | 'adjustment'>('purchase'); const [reason, setReason] = useState(''); const [menuItemId, setMenuItemId] = useState(''); const [recipeIngredientId, setRecipeIngredientId] = useState(''); const [recipeQuantity, setRecipeQuantity] = useState(''); const [recipeLines, setRecipeLines] = useState<{ ingredientId: string; quantity: number; wastePercent: number }[]>([])
  async function submit(event: React.FormEvent) { event.preventDefault(); if (feedback.pending) return; feedback.setPending(); try { await createIngredient({ data: { tenantId, name, unit: unit as 'g' | 'kg' | 'ml' | 'l' | 'unit', costCentsPerUnit: Number(cost), minimumStock: Number(minimum), allergens: [], isVegan: false } }); setName(''); setCost(''); setMinimum(''); feedback.setSuccess('Ingrediente creado.'); onDone() } catch { feedback.setError('No se ha podido crear el ingrediente.') } }
  return <section className="space-y-6"><Card><CardHeader><CardTitle>Ingredientes e inventario</CardTitle></CardHeader><CardContent className="space-y-4"><form className="grid gap-3 md:grid-cols-5" onSubmit={submit}><Field><FieldLabel htmlFor="ingredient-name">Nombre</FieldLabel><Input id="ingredient-name" onChange={(e) => setName(e.target.value)} required value={name} /></Field><Field><FieldLabel htmlFor="ingredient-unit">Unidad</FieldLabel><select className="border-input h-10 rounded-md border bg-background px-3 text-sm" id="ingredient-unit" onChange={(e) => setUnit(e.target.value)} value={unit}><option>kg</option><option>g</option><option>l</option><option>ml</option><option>unit</option></select></Field><Field><FieldLabel htmlFor="ingredient-cost">Coste céntimos/unidad</FieldLabel><Input id="ingredient-cost" min="0" onChange={(e) => setCost(e.target.value)} required type="number" value={cost} /></Field><Field><FieldLabel htmlFor="ingredient-minimum">Mínimo</FieldLabel><Input id="ingredient-minimum" min="0" onChange={(e) => setMinimum(e.target.value)} required type="number" value={minimum} /></Field><Button className="self-end" type="submit">Añadir</Button></form><FormFeedback pendingLabel="Guardando…" state={feedback.state} /></CardContent></Card><Card><CardHeader><CardTitle>Receta y escandallo</CardTitle></CardHeader><CardContent className="space-y-4"><Field><FieldLabel htmlFor="recipe-menu-item">Producto</FieldLabel><select className="border-input h-10 rounded-md border bg-background px-3 text-sm" id="recipe-menu-item" onChange={(e) => setMenuItemId(e.target.value)} value={menuItemId}><option value="">Seleccionar producto…</option>{menuItems.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></Field><form className="flex flex-wrap items-end gap-3" onSubmit={(e) => { e.preventDefault(); if (!recipeIngredientId) return; setRecipeLines([...recipeLines, { ingredientId: recipeIngredientId, quantity: Number(recipeQuantity), wastePercent: 0 }]); setRecipeIngredientId(''); setRecipeQuantity('') }}><Field><FieldLabel htmlFor="recipe-ingredient">Ingrediente</FieldLabel><select className="border-input h-10 rounded-md border bg-background px-3 text-sm" id="recipe-ingredient" onChange={(e) => setRecipeIngredientId(e.target.value)} value={recipeIngredientId}><option value="">Seleccionar…</option>{ingredients.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></Field><Field><FieldLabel htmlFor="recipe-quantity">Cantidad</FieldLabel><Input id="recipe-quantity" min="0.0001" onChange={(e) => setRecipeQuantity(e.target.value)} required type="number" value={recipeQuantity} /></Field><Button type="submit">Añadir a receta</Button></form><ul className="text-sm">{recipeLines.map((line, index) => <li key={`${line.ingredientId}-${index}`}>{ingredients.find((item) => item.id === line.ingredientId)?.name}: {line.quantity}</li>)}</ul><Button disabled={!menuItemId || recipeLines.length === 0} onClick={() => void replaceRecipe({ data: { tenantId, menuItemId, lines: recipeLines } }).then(() => { feedback.setSuccess('Receta guardada.'); setRecipeLines([]); onDone() })} type="button">Guardar receta</Button></CardContent></Card><Card><CardHeader><CardTitle>Registrar movimiento</CardTitle></CardHeader><CardContent><form className="grid gap-3 md:grid-cols-4" onSubmit={(e) => { e.preventDefault(); void (async () => { if (feedback.pending) return; feedback.setPending(); try { await addInventoryMovement({ data: { tenantId, venueId, ingredientId, kind: movementKind, quantity: Number(movementQuantity), reason } }); setMovementQuantity(''); setReason(''); feedback.setSuccess('Movimiento registrado.'); onDone() } catch { feedback.setError('No se ha podido registrar el movimiento.') } })() }}><Field><FieldLabel htmlFor="movement-ingredient">Ingrediente</FieldLabel><select className="border-input h-10 rounded-md border bg-background px-3 py-2 text-sm" id="movement-ingredient" onChange={(e) => setIngredientId(e.target.value)} required value={ingredientId}><option value="">Seleccionar…</option>{ingredients.map((ingredient) => <option key={ingredient.id} value={ingredient.id}>{ingredient.name}</option>)}</select></Field><Field><FieldLabel htmlFor="movement-kind">Tipo</FieldLabel><select className="border-input h-10 rounded-md border bg-background px-3 py-2 text-sm" id="movement-kind" onChange={(e) => setMovementKind(e.target.value as typeof movementKind)} value={movementKind}><option value="purchase">Compra (+)</option><option value="waste">Merma (-)</option><option value="adjustment">Ajuste (+/-)</option></select></Field><Field><FieldLabel htmlFor="movement-quantity">Cantidad (+/-)</FieldLabel><Input id="movement-quantity" onChange={(e) => setMovementQuantity(e.target.value)} required type="number" value={movementQuantity} /></Field><Field><FieldLabel htmlFor="movement-reason">Motivo</FieldLabel><Input id="movement-reason" onChange={(e) => setReason(e.target.value)} required value={reason} /></Field><Button className="md:col-span-4 md:justify-self-end" type="submit">Registrar movimiento</Button></form></CardContent></Card><Card><CardHeader><CardTitle>Stock actual</CardTitle></CardHeader><CardContent><Table><TableHeader><TableRow><TableHead>Ingrediente</TableHead><TableHead>Unidad</TableHead><TableHead>Stock</TableHead><TableHead>Mínimo</TableHead><TableHead>Estado</TableHead></TableRow></TableHeader><TableBody>{ingredients.map((ingredient) => { const current = stock.stock[ingredient.id] ?? 0; const low = stock.lowStockIngredientIds.includes(ingredient.id); return <TableRow key={ingredient.id}><TableCell>{ingredient.name}</TableCell><TableCell>{ingredient.unit}</TableCell><TableCell>{current}</TableCell><TableCell>{ingredient.minimumStock}</TableCell><TableCell>{low ? <span className="text-destructive">Bajo mínimo</span> : 'Correcto'}</TableCell></TableRow> })}</TableBody></Table></CardContent></Card></section>
}
