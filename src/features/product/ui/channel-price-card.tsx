import { Button, Card, CardContent, CardHeader, CardTitle, Field, FieldLabel, Input, useFormFeedback } from '@doscientos/ui'
import { useState } from 'react'
import { setMenuChannelPrice } from '@/features/menu'

export function ChannelPriceCard({ menuItems, tenantId, onDone }: { menuItems: { id: string; name: string }[]; tenantId: string; onDone: () => void }) {
  const feedback = useFormFeedback(); const [itemId, setItemId] = useState(''); const [channel, setChannel] = useState<'room' | 'web' | 'delivery' | 'takeaway'>('web'); const [price, setPrice] = useState('')
  return <Card><CardHeader><CardTitle>Precios por canal</CardTitle></CardHeader><CardContent><form className="flex flex-wrap items-end gap-3" onSubmit={(event) => { event.preventDefault(); if (feedback.pending) return; feedback.setPending(); void setMenuChannelPrice({ data: { tenantId, menuItemId: itemId, channel, priceCents: Math.round(Number(price) * 100) } }).then(() => { feedback.setSuccess('Precio guardado.'); setPrice(''); onDone() }).catch(() => feedback.setError('No se ha podido guardar el precio.')) }}><Field><FieldLabel htmlFor="channel-price-item">Producto</FieldLabel><select className="border-input h-10 rounded-md border bg-background px-3 text-sm" id="channel-price-item" onChange={(e) => setItemId(e.target.value)} required value={itemId}><option value="">Seleccionar…</option>{menuItems.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></Field><Field><FieldLabel htmlFor="channel-price-channel">Canal</FieldLabel><select className="border-input h-10 rounded-md border bg-background px-3 text-sm" id="channel-price-channel" onChange={(e) => setChannel(e.target.value as typeof channel)} value={channel}><option value="room">Sala</option><option value="web">Web</option><option value="delivery">Delivery</option><option value="takeaway">Takeaway</option></select></Field><Field><FieldLabel htmlFor="channel-price-value">Precio (€)</FieldLabel><Input id="channel-price-value" min="0" onChange={(e) => setPrice(e.target.value)} required step="0.01" type="number" value={price} /></Field><Button type="submit">Guardar precio</Button></form></CardContent></Card>
}
