# Auditoría funcional y UX — 2026-09-13

Sesión manual autenticada sobre `http://localhost:3000/t/la-fonda-demo`.

## Bugs confirmados

- **P0 — corregido** TPV, Plano, Servicio y Reservas cargan el contexto validado del local mediante `loadVenueRouteContext`, con `tenantId`/`venueId` definidos.
- **P1 — corregido** `src/routes/t.$slug.facturas.tsx` renderizaba `InvoiceListPage invoices={[]}`: ahora carga el resumen real de facturación y muestra el estado vacío solo cuando no hay facturas.
- **P1 — corregido** Facturas, Comunicaciones y Equipo tienen loaders directos, estados de carga compartidos y boundary operativo estable; el overlay de HMR no forma parte del render de producción.
- **P2 — corregido** TPV/Servicio/Plano/Reservas muestran un boundary accionable con reintento en lugar del error técnico serializado.
- **P1 — corregido** La entrada del local usa el contexto validado del venue y el reporte del TPV calcula siempre el inicio y fin del día antes de cargarlo.
- **P1 — corregido** `e2e/smoke.spec.ts` cubre navegación pública y autenticada sin error boundary ni cuerpo vacío; también cubre el disparador accesible de navegación móvil.
- **P1 — corregido** `operationalTenantMiddleware` bloquea también el acceso directo cuando una suscripción en prueba no tiene método de pago; la UI ofrece el acceso a suscripción.
- **P2 — corregido** El dashboard ya no afirma que todos los sistemas funcionan; comunica únicamente que no hay acciones pendientes cuando el loader ha completado correctamente.
- **P2 — corregido** Facturas identifica explícitamente `Entorno no productivo: VERI*FACTU en pruebas`.

## Mejoras UX

- **P1 — corregido** Carta muestra un estado vacío y mantiene visible el formulario para crear la primera categoría y sus platos.
- **P1 — corregido** La ficha pública muestra un estado vacío accionable y no renderiza el formulario ni un botón de reserva cuando no hay turnos.
- **P2 — corregido** El selector público “Momento” incluye un placeholder accesible.
- **P2 — corregido** La navegación lateral solo se muestra desde `lg` y las pestañas del local solo en móvil; no se duplican en escritorio.
- **P2 — corregido** Las rutas tenant usan un skeleton común con `aria-busy` durante navegaciones lentas o directas.
- **P2 — corregido** La cobertura E2E añade una comprobación móvil del menú, visibilidad y foco del disparador de navegación.
- **P2 — corregido** La disponibilidad pública distingue error de consulta de ausencia real de mesas y ofrece reintento/contacto con el restaurante.
- **P2 — corregido** La ficha pública recibe `defaultLocale` desde el perfil del restaurante y ya no usa el idioma del navegador como valor inicial implícito.
- **P1 — corregido** La ficha pública tiene una rama determinista: sin servicios no renderiza formulario ni botón de reserva; con servicios muestra el formulario y su estado de disponibilidad.
- **P1 — corregido** `/ajustes` tiene timeout de 3 segundos para `serviceWorker.ready`, muestra estado de comprobación y un diagnóstico visible de indisponibilidad/error.
- **P1 — corregido** `/t/$slug/ajustes` reutiliza el shell completo del restaurante; desde una ruta tenant la sidebar conserva locales, operativa, gestión, suscripción y cuenta.
- **P0 — corregido** `pnpm typecheck` pasa con `@doscientos/pwa` y las preferencias de notificaciones.
- **P2 — corregido** `locale-preference.tsx` usa una suscripción externa compatible con SSR para hidratar la preferencia de idioma sin un `setState` síncrono en un effect; también tolera almacenamiento local no disponible.
- **P1 — corregido en código** Auditoría de plataforma e historial de recomendaciones ya exponen contratos paginados con filtros y total; las lecturas secundarias restantes son resúmenes acotados de detalle, no colecciones principales.
- **P1 — corregido** Tarjetas regalo y fidelización usan paginación backend, búsqueda o total, y filtros por tenant.
- **P0 — corregido en migración pendiente de aplicar** El cobro con tarjeta regalo respeta `operation_id` y no vuelve a descontar saldo en reintentos.
- **P0 — corregido en migración pendiente de aplicar** La operación de tarjeta regalo queda vinculada al movimiento de la tarjeta y al pago original; reutilizar la misma clave con otra tarjeta ya no puede producir un descuento ambiguo.
- **P1 — corregido** Pedidos online conservan los pedidos completados en una columna terminal y bloquean acciones sin efecto.
- **P1 — corregido** La subida de documentos de compras valida server-side que `venue_id` pertenece al tenant antes de almacenar el fichero.
- **P1 — corregido** La configuración de ingredientes impide marcar como vegano un ingrediente con leche, huevos, pescado, crustáceos o moluscos; los alérgenos compatibles sí se conservan y se heredan a recetas/carta.
- **P2 — revisado** Los `limit()` restantes corresponden a historiales embebidos de detalle (eventos de reserva, snapshots de relevo y notas agregadas), no a listados principales; permanecen acotados intencionadamente para proteger la operativa.
- **P1 — en curso** Se ha creado un catálogo comercial único de módulos con precios, dependencias y acceso por rol, además de entitlements de plan, overrides por tenant gestionables por plataforma y solicitudes de activación. Falta conectar la administración y los teasers comerciales a las pantallas.

## Próximo ciclo

1. Ejecutar la suite E2E autenticada con `E2E_STORAGE_STATE` de una cuenta de pruebas.
2. Revisar acciones reales de TPV, Plano, Servicio, Reservas, Carta, Equipo y Comunicaciones.
3. Ejecutar build/quality completo y resolver cualquier fallo de producción restante.
4. Pasada responsive, accesibilidad y permisos por rol.

## Verificación adicional 13/09/2026

- **P1 — reproducir/corregir** En la sesión visual local, `/t/la-fonda-demo/l/principal/productos` mostró primero un error de validación por `venueId` ausente y, tras recargar, un boundary genérico. El loader actual ya envía `tenantId` y `venueId` en sus lecturas; falta reiniciar/verificar el servidor local con el bundle actual y capturar qué server function devuelve el error antes de marcarlo como corregido.

- **P1 — corregido** El TPV tenant llegó a mostrar el boundary “No se ha podido cargar esta pantalla” por la serialización de server actions durante SSR/dev; sus lecturas principales se han extraído a repositorios server-side y se ha verificado el render correcto en navegador.
- **P1 — corregido** Servicio y TPV cargan sus lecturas SSR principales mediante los repositorios server-side con el token ya validado, evitando la pérdida de `venueId` del resolver de server actions en desarrollo. El TPV se verificó visualmente sin error boundary; caja e informes quedan aislados como métricas opcionales.

## Cobertura E2E añadida

- Se añadió `playwright.config.ts` y `e2e/smoke.spec.ts` con una prueba pública no mutante y smoke tests autenticados para las rutas principales.
- Los tests autenticados solo se activan si se proporciona `E2E_STORAGE_STATE`, que debe proceder de una cuenta/tenant de pruebas y nunca de producción.
- La suite está diseñada para una única base de datos: no crea, edita ni elimina datos; solo comprueba renderizado, errores boundary y estados visibles.
- Chromium ya está instalado y el smoke público pasa; los tests autenticados quedan saltados hasta proporcionar `E2E_STORAGE_STATE` de una cuenta de pruebas.
- No se afirma aquí ninguna aplicación remota de migraciones: las migraciones locales deben revisarse y verificarse contra el proyecto Supabase destino antes de aplicarse.
